import { defineConfig } from 'vite'

export default defineConfig({
  // Specify the base path for your GitHub Pages repository.
  // For example, if you host at username.github.io/sv-simulator/, this must be '/sv-simulator/'
  // If you host at a custom domain or the root username.github.io, you can comment this out or set it to '/'
  base: '/sv-simulator/',
})
